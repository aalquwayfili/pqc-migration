{
  "local_prompt": "You are working in /work, a Go module named `filesigner`.\n\ncmd/rsa-signer/main.go is a command-line file signer that uses RSA-2048 with PKCS#1 v1.5. Migrate it to ML-DSA-44 by creating cmd/mldsa-signer/main.go: a program with the same three commands that follows CONTRACT.md exactly. CONTRACT.md is the specification; where it and the RSA code disagree, the contract wins. Do not modify cmd/rsa-signer.\n\nYour environment:\n- There is no network access.\n- Go 1.27.1 is on PATH. github.com/cloudflare/circl v1.6.5, already required in go.mod, is available from an offline module proxy. No other third-party modules are available.\n- Build with: go build -o /tmp/filesigner-mldsa ./cmd/mldsa-signer\n- OpenSSL 3.5.8, with ML-DSA-44 support, is on PATH as `openssl` while you develop. It is not available to your program during final scoring: the submitted program runs without OpenSSL, so it must not call it.\n{{FEEDBACK}}\nBefore you submit, build your program, test it against the requirements in CONTRACT.md, and repair every failure you find. Submit only after that.\n\nYou may run at most {{STEP_LIMIT}} commands. After you finish, cmd/mldsa-signer will be built from /work and tested independently against CONTRACT.md, using inputs you have not seen.\n",
  "local_feedback": "- A checker is available. Send the command `check`, exactly that and nothing else, to build the current /work and run the contract checker against it. It prints every check's name, its result (PASS, FAIL, DEPMISS or ERROR) and a short detail. Each run uses new inputs. Each `check` counts as one of your commands.\n",
  "native_prompt": "You are working in /work, a Go module named `filesigner`.\n\ncmd/rsa-signer/main.go is a command-line file signer that uses RSA-2048 with PKCS#1 v1.5. Migrate it to ML-DSA-44 by creating cmd/mldsa-signer/main.go: a program with the same three commands that follows CONTRACT.md exactly. CONTRACT.md is the specification; where it and the RSA code disagree, the contract wins. Do not modify cmd/rsa-signer.\n\nYour environment:\n- Your shell has no usable network access: nothing can be downloaded, and no module can be fetched.\n- Go 1.27.1 is on PATH. github.com/cloudflare/circl v1.6.5, already required in go.mod, is available from an offline module proxy. No other third-party modules are available.\n- Build with: go build -o /tmp/filesigner-mldsa ./cmd/mldsa-signer\n- OpenSSL 3.5.8, with ML-DSA-44 support, is on PATH as `openssl` while you develop. It is not available to your program during final scoring: the submitted program runs without OpenSSL, so it must not call it.\n{{FEEDBACK}}\nBefore you finish, build your program, test it against the requirements in CONTRACT.md, and repair every failure you find. Finish only after that.\n\nYou may take at most {{STEP_LIMIT}} tool actions in total; every shell command and every file edit counts as one. When you are done, end your turn with a short final message; that is the submission. After you finish, cmd/mldsa-signer will be built from /work and tested independently against CONTRACT.md, using inputs you have not seen.\n",
  "native_feedback": "- A checker is available. Run the shell command `check`, exactly that and nothing else, to build the current /work and run the contract checker against it. It prints every check's name, its result (PASS, FAIL, DEPMISS or ERROR) and a short detail. Each run uses new inputs. Each `check` counts as one of your tool actions.\n"
}
